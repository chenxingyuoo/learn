package config

const (
	AppName = "microservices-demo"

	TokenSecret = "token secret key"

	// 日志文件
	// AppAccessLogName = "log/" + AppName + "-access.log"
	// AppErrorLogName  = "log/" + AppName + "-error.log"
	// AppGrpcLogName   = "log/" + AppName + "-grpc.log"
)
