/**
 * Created by chenxingyu on 2017/5/5.
 */

//getFile("file_path");
// getFileFromNet("url");

// 如果以上两个任务的时间分别为m和n。采用同步方式的程序要完成这两个任务的时间总花销会是m + n。
// 但是如果是采用异步方式的程序，在两种I/O可以并行的状况下（比如网络I/O与文件I/O），时间开销将会减小为 max(m, n)。

